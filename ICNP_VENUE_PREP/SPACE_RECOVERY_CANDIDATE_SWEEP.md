# Space-Recovery Candidate Sweep

This note records a surgical space-saving review of the active ICNP draft. It is an approval queue, not an implementation pass: no manuscript prose is changed by this note. Each item states what would be removed or replaced and the proposed replacement text.

## Baseline

- **Date:** 2026-05-22
- **Pulled source:** `cd12aa4` (`Updates from Overleaf`)
- **Active draft:** `ICNP_2026_venue_draft.tex`
- **Build check:** `latexmk -pdf -interaction=nonstopmode -halt-on-error ICNP_2026_venue_draft.tex`
- **Current PDF:** 17 pages total; Conclusion remains on page 10 and References start on page 11
- **Scope:** main-body space recovery only; appendix trimming is deferred unless main-body cuts are insufficient

## Recommended First-Pass Cuts

| ID | Location | Action | Remove / Replace | Proposed replacement | Reason |
|---|---|---|---|---|---|
| S-01 | `ICNP_2026_venue_draft.tex:189` | Replace | `efficiency floors\footnote{Defined in \Cref{tab:notation_metrics}} by 15.6~pp` | `efficiency floors by 15.6~pp` | Removes a rendered footnote; the notation table already defines floor and is referenced elsewhere. |
| S-02 | `ICNP_2026_venue_draft.tex:228` | Replace | `In contrast, we employ a stochastic approach. The key difference is evaluation scope: Huang \etal test a specific adversarial-neural policy, while our study compares multiple policy families under the same topology, threat regimes, allocator policies, and replay-capacity settings. This lets us attribute robustness differences to the policy--allocator--capacity interaction rather than to incompatible experimental assumptions.` | `Our contrast is evaluation scope: Huang \etal test one adversarial-neural policy, while we compare multiple policy families under matched topology, threat, allocator, and replay settings, isolating the policy--allocator--capacity interaction.` | Removes the awkward stochastic-approach sentence and compresses the contrast without changing the claim. |
| S-03 | `ICNP_2026_venue_draft.tex:291` | Replace | `The canonical 4-path internal topology uses the fixed baseline split $(T_1,T_2,T_3,T_4)=(8,10,8,9)$, totaling 35 qubits. This internal 35-qubit setting defines the controlled baseline in this section, not a global limit of the broader framework, which also supports topology-specific budgets for external testbeds. We evaluate four allocator policies: Fixed, ThompsonSampling, DynamicUCB, and Random---spanning static, uncertainty-aware, optimistic, and uninformed allocation behavior. Fixed maintains the baseline split; ThompsonSampling samples path utilities from posterior estimates; DynamicUCB redistributes capacity using upper-confidence estimates; and Random assigns capacity uniformly as a control. These allocator choices instantiate the deployment-configuration dimension identified in \cref{sec:RelatedWork}.` | `The internal topology uses the baseline split $(T_1,T_2,T_3,T_4)=(8,10,8,9)$, totaling 35 qubits; this is a controlled internal baseline, not a global framework limit. We compare Fixed, ThompsonSampling, DynamicUCB, and Random allocators, spanning static, posterior-sampling, optimistic, and uninformed allocation behavior.` | Keeps the important 35-qubit clarification but cuts repeated allocator mechanism detail. |
| S-04 | `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:33` and `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:125` | Replace | Network-configuration paragraph plus standalone `Resource separation` mini-section. | `We use the four-path diamond topology in \cref{fig:network_topology,subsec:topology}; the internal 35-qubit budget is fixed while allocator policies vary per-path splits. Replay scaling changes only classical memory, separating learning-memory effects from quantum hardware limits.` | Removes duplicated topology and fixed-budget statements while preserving the hardware-vs-replay distinction. |
| S-05 | `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:116` | Replace | Displayed equations `T_b=sF_b,\qquad T=sF_c.` plus surrounding setup. | `Replay capacity is scaled by $s\in\{1,1.5,2\}$ under base-horizon anchoring $T_b=sF_b$ and current-horizon anchoring $T=sF_c$; this sweep tests when added replay helps estimation versus increasing adversarial predictability.` | Converts a vertical equation block into one inline sentence. |
| S-06 | `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:131` | Remove | `\smallTitle{Adversarial threat taxonomy}` paragraph. | No replacement; keep `\smallTitle{Scenario specifications}` and the table. | The scenario table already instantiates the threat settings; the extra setup sentence is redundant. |
| S-07 | `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:156` | Replace | `\smallTitle{Bandit-policy portfolio}` paragraph plus separate `Key bandit-policy features` lead. | `\smallTitle{Bandit-policy features} The portfolio spans classical, adversarial, contextual/neural, pursuit-based, and predictive policies; CPursuitNeuralUCB adds topology/channel context, iCPursuitNeuralUCB adds ARIMA$(1,0,1)$ forecasts with warmup $n\in\{50,100\}$, and Oracle provides the ideal-reference normalization.` | Merges two small sections into one without changing definitions. |
| S-08 | `ICNP_VENUE_PREP/STUDY_DESIGN_VALIDATED_STAGING.tex:207` | Replace | Full paragraph plus displayed efficiency and CV equations. | `Overall, the design covers 552 unique configurations with fixed seeds and repeated-run counts $S\in\{3,5,8,10\}$; primary results report 3- and 5-run averages. We report Oracle-normalized efficiency and scenario-level CV, using 10{,}000-sample bootstrap intervals; differences above 5~pp with non-overlapping intervals are practically significant, and head-to-head wins require a $>2$~pp lead.` | Removes GPU-hour detail and two displayed formulas; keep if reviewers need formulas, otherwise this is a high-value vertical-space recovery. |
| S-09 | `ICNP_VENUE_PREP/RESULTS_VALIDATED_STAGING.tex:18` and `ICNP_VENUE_PREP/RESULTS_VALIDATED_STAGING.tex:118` | Replace | Long RQ1/RQ2 aggregation paragraphs. | `RQ1 filters EXP-family, CMAB, and iCMAB corpora to Stochastic/default-allocator runs and aggregates across horizons, replay scales, replay semantics, and 3-/5-run suites.` / `RQ2 uses the Markov, Adaptive, and OnlineAdaptive default-allocator slice under the same aggregation scope, comparing the strongest CMAB/iCMAB representatives with EXP-family baselines.` | Keeps reproducibility but removes repeated enumeration. |
| S-10 | `ICNP_VENUE_PREP/RESULTS_VALIDATED_STAGING.tex:125` | Replace | Full robustness-floor caption. | `Worst-case floors separate stable contextual policies from fragile adversarial-first baselines: \texttt{iCEpsGreedy} has the strongest floor, \texttt{CPursuit} the best average, and \texttt{EXPNeuralUCB} remains fragile under Markov/Adaptive/OnlineAdaptive threats.` | The regenerated plot already labels floor/mean/peak; caption can be shorter. |
| S-11 | `ICNP_VENUE_PREP/RESULTS_VALIDATED_STAGING.tex:253` | Replace | RQ3b replay-scaling setup paragraph. | `The RQ3b claim is anchored by the validated ThompsonSampling/$T_b$/OnlineAdaptive slice: efficiency drops 4.35~pp from $s{=}1$ to $s{=}1.5$ and recovers 5.96~pp at $s{=}2$; \cref{tab:rq3b_master_capacity_scaling} keeps fixed-Random results only as stress-control evidence.` | Preserves the Random-as-supporting-evidence rule while cutting repeated explanation. |
| S-12 | `ICNP_VENUE_PREP/RESULTS_VALIDATED_CROSS_TESTBED.tex:80` | Replace | Four-sentence cross-testbed/model-family wrap-up. | `Together, \cref{fig:cross_testbed_confirmation,tab:testbed_comparison_summary,tab:model_family_comparison_summary} show that pursuit--neural policies lead average robustness across external testbeds, while topology complexity reduces efficiency and can change per-configuration winners; full evidence remains in \cref{tab:testbed_comparison_full,tab:model_family_comparison_full}.` | Removes duplicated references and keeps the main claim plus appendix pointer. |
| S-13 | `ICNP_VENUE_PREP/DISCUSSION.tex:7` | Replace | Full appendix-numerical paragraph. | `The full appendix tables support the same hierarchy: \texttt{iCPursuitNeuralUCB} leads the internal family ranking and has the highest average efficiency across all four external testbeds, even when individual configuration wins vary.` | Results already carry the exact values; Discussion can synthesize rather than repeat all numbers. |
| S-14 | `ICNP_VENUE_PREP/DISCUSSION.tex:38` | Replace | Full three-principle implications paragraph. | `For adversarial quantum networks, the deployment rule is direct: treat context-aware routing as baseline infrastructure, tune replay capacity jointly with allocator and threat regime, and use lightweight threat-aware meta-policies to adapt allocator/replay settings from observed efficiency and variability.` | Preserves the design principles while removing repeated first/second/finally expansion. |
| S-15 | `ICNP_VENUE_PREP/FUTURE_WORK.tex:5` and `ICNP_VENUE_PREP/FUTURE_WORK.tex:20` | Replace | Long Limitations paragraph and long Future Work paragraph. | `Limitations are topology scale, software-defined testbed fidelity, replay-capacity coverage, and ARIMA-only forecasting; these bounds motivate larger and higher-fidelity testbeds, broader replay sweeps, and richer temporal models. Future work will standardize cross-testbed benchmarking, extend routing/allocation spaces, and replace the heuristic meta-policy with learned threat detection and configuration selection.` | High-value page-end cut; keeps all four limitation categories and all future-work directions. |

## Deferred or Not Recommended

- **Do not cut the locked Conclusion first.** `ICNP_VENUE_PREP/CONCLUSION.tex` is explicitly marked as locked validated wording; use it only as an emergency cut target after approving higher-value cuts above.
- **Do not remove main-body claim figures before prose/caption cuts.** The current page pressure is better addressed through repeated setup prose and long captions.
- **Do not reintroduce the Introduction threat table unless space is recovered and advisors explicitly want it.** It is already commented out for page budget.

## Suggested Approval Order

1. **Low-risk source/caption cuts:** S-01, S-02, S-06, S-09, S-10, S-11, S-12.
2. **Medium-risk compression:** S-03, S-04, S-05, S-07, S-13, S-14.
3. **High-value page-end compression:** S-08 and S-15.
4. **Emergency only:** locked conclusion shortening or figure/table removal.
